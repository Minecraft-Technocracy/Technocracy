rootProject.name = 'technocracy.megastructures'

boolean inComposite = gradle.parent != null

if (!inComposite) {
    includeBuild "../technocracy.foundation"
    includeBuild "../technocracy.astronautics"
}